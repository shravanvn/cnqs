#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

std::string padString(const std::string &inputString, int padding = 4);

std::string PadString(const std::string &inputString, int padding = 4);

#endif
